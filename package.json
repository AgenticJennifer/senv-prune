{
  "name": "senv-prune",
  "version": "0.4.0",
  "private": true,
  "description": "Safely deduplicate dotenv files and verify API credential status without exposing values.",
  "bin": {
    "senv-prune": "./senv-prune.sh"
  },
  "scripts": {
    "check": "bash scripts/check-shell.sh",
    "test": "node --test tests/*.test.mjs && bash tests/test-senv-prune.sh"
  }
}
